#posts
	- @posts.each do |post|
		.post	
			%h2.title= link_to post.title, post			

			%p.image
				- if post.image?
					= image_tag post.image(:thumb)			
			%p.date 
				Published at
				=post.created_at.strftime("%H:%M %d-%b-%y")
				by
				= post.user.email